[tool.ruff]
target-version = "py313"
line-length = 80
extend-exclude = [
    ".venv",
    "target",
    "dbt_packages",
    "logs",
]

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B", "SIM"]

[tool.ruff.format]
quote-style = "double"
